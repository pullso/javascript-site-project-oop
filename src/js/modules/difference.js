export default class Difference {  constructor (container, items) {    this.container = document.querySelector(container)    this.items = this.container.querySelectorAll(items)    this.itemsCounter = 0  }    bindTriggers () {    this.container.querySelector('.plus').addEventListener('click', () => {      const card = this.items[this.itemsCounter]      if (this.itemsCounter !== this.items.length - 2) {        this.itemsCounter += 1      } else {        this.items[this.itemsCounter + 1].remove()      }            card.classList.add('animated', 'fadeIn')      card.style.display = 'flex'    })  }    hideItems () {    this.items.forEach((item, i, arr) => {      if (i !== arr.length - 1) {        item.style.display = 'none'      }    })  }    init () {    this.hideItems()    this.bindTriggers()  }}